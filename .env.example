# Copy to .env and fill. Never commit the real .env.
# AI keys are read at runtime; they are stored encrypted in the DB once entered in Settings.
GEMINI_API_KEY=
OPENAI_API_KEY=
# Optional overrides
OFIS_LOG_LEVEL=INFO
